# Site

Static frontend, published via GitHub Pages, that loads the `graph-wasm`
module and the `graph.sqlite` database so viewers can run the same build
queries in-browser.

Not yet implemented — placeholder until `graph-wasm` has real queries to
call.
