//! Reads `characters/` and `knowledge/` and builds the SQLite graph
//! (`graph/data/graph.sqlite`) that `graph-wasm` queries in the browser.
//!
//! Not yet implemented — parsing/graph-construction is a follow-up phase.

fn main() {
    println!("build-graph: not yet implemented");
}
